import { DurationSincePipe } from './duration-since.pipe';

describe('DurationSincePipe', () => {
  it('create an instance', () => {
    const pipe = new DurationSincePipe();
    expect(pipe).toBeTruthy();
  });
});
