{
    "allowedRoutes": ["/starterApp/configuration"],
    "allowedButtonIds": ["refresh", "configure"],
    "configuration": [
        {
            "roleId": "Administrator",
            "routes": ["/starterApp/configuration"],
            "buttonIds": ["refresh","configure"]
        },
        {
            "roleId": "User",
            "routes": [],
            "buttonIds": ["refresh"]
        }
    ]
}
